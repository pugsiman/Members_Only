<% if @post.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      There are <%= pluralize(@post.errors.count, "problem") %> with your form.
    </div>
    <ul>
    <% @post.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
